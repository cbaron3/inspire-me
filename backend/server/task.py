def my_job():
    # Get list of all users with filter confirmed True
    # For every user
        # Check if time matches 
            # If matches, send message, and track that it was sent (last day sent)
        # If not, continue
    print('Hello World')
